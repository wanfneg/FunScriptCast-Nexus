# AI 字幕迭代日志（iteration_shturl）

> 开始：2026-09-16 04:45 ｜ 上限 3.5 小时（至 ~08:15）
> 规则：每轮增量小改进（1~2 个问题）、改动前 git 快照、改完跑脚本测试、
> 每轮最多联网搜索 3 次、只借鉴思路不复制代码。

## 基线（Round 0）

- 是否联网检索：否
- 代码变更：无（提交基线快照 9e558a1，包含此前全部修复）
- 当前已知状态：流式端点退役、10s 块 + 离线 VAD 端点、假名计数隐藏、
  引号剥离、碎片去重、比例摊时、翻译上下文承接（**未验证效果**）、
  ASR 热词追加上一句（**未验证效果**）
- 待验证/待优化清单（按优先级）：
  1. 翻译上下文承接实际效果未验证
  2. ASR 热词追加上一句的效果与回显风险未验证
  3. 评测流程手工步骤多 → 需要一键脚本
  4. 整片字幕缓存无管线版本标记（旧管线字幕会当基线加载）
  5. server_app 无代码版本标识（陈旧实例难以从日志发现）
  6. 免费兜底偶发繁体输出
  7. 翻译质量天花板（3b 模型）——云端方案待 API Key


## Round 1（04:50–05:10）：评测自动化 + 上下文承接首验

- 是否联网检索：否
- 搜索关键词：无
- 参考开源项目：无（复用既有工具）
- 代码变更：新增 tests/run_eval.py（一键评测：自起隔离测试服务 :8759 → 推流 → 对照人工字幕；
  端口健康则复用实例；修复离线脚本 int 参数与遗漏 --url 两个小问题）
- 借鉴点：无
- 测试结果（90–270s 切片，离线端点 10s 块，上下文承接开启）：
  硬缺陷 0/0/0（空译文/假名/夹英文）；时序中位 −270ms（±0.5s 内 65.2%）；
  配对 23 对；内容覆盖 0.267（可信 16 对）；端到端 23.5s
- 风险点：run-to-run ASR/翻译非确定性导致软指标波动，比较需同窗口
- 结论：上下文承接无回归，进入常规基线

## Round 2（04:50–05:05）：整片字幕缓存指纹补代码签名 + 翻译配置

- 是否联网检索：否
- 代码变更：host_server._config_fingerprint 补两段——
  ① 管线源码签名（vendor/subtitle/*.py 内容 sha256，任一判据/策略/引擎修复 → 缓存键变化，
  旧管线差字幕不再"当基线加载"）；② translate 配置段进指纹（换翻译后端/模型后旧译文失效）
- 借鉴点：无（针对本轮 dist-app 陈旧快照暴露的缓存失效盲区）
- 验证：临时目录模拟"代码修复"→ 指纹变化 ✓；touch（仅 mtime）→ 指纹不变 ✓（内容哈希的预期行为）
- 风险点：源码签名含全部 *.py → 任何小改动都会让全片缓存失效（有意的保守策略；
  代价是改代码后同一视频需重跑识别，属正确行为）

## Round 3–4（04:52–05:10）：ASR 确定性验证 + 翻译模型 A/B（3b vs 7b）

- 是否联网检索：否
- 代码变更：无（纯实验；config 临时改 7b 后已还原 3b）
- 实验设计：同段音频热词 A/B（有/无上一句热词 × 2 次）；同切片 3b vs 7b 全流程评测
- 实验结论：
  1. Qwen3-ASR 为 argmax 贪心解码（audio.cpp thinker.cpp:821），同请求输出确定 ✓
  2. 热词追加上一句在干净语音窗口无害（输出一致、无回显触发）✓
  3. 7b 翻译：内容覆盖 0.302 vs 3b 0.267（小幅提升），但请求中位 0.9s→7.8s（8.7×），
     实时模式不可接受；逐句对比多数行持平 → **维持 3b**
- 风险点：7b 试验时 audiocpp(2.4GB)+ollama 7b(~4.7GB) 同卡 8GB，余量极小
- 下一步：全片基线 → 联网检索 ja→zh 专用翻译模型（SAKURA 系）→ 试验

## Round 6（05:10–05:25）：联网检索 + Sakura 翻译特化模型接入（逐句 MT 模式）

- 是否联网检索：是（1 次）
- 搜索关键词：SAKURA 日语翻译模型 ollama galgame 日中 qwen2.5 GGUF
- 参考开源项目：SakuraLLM/SakuraLLM（https://github.com/SakuraLLM/SakuraLLM）
  —— 轻小说/Galgame 日中翻译特化模型，Qwen2.5 底座，GGUF + Ollama 支持，
  官方提示词/采样参数（temp 0.1 / top_p 0.3）取自其 README
- 借鉴点：领域微调翻译模型 + 逐句纯文本直翻模式（不服从 JSON 批量指令）
- 代码变更：translate_engine 新增逐句 MT 模式（mt_system/mt_user_prefix 配置非空即启用：
  单句直翻 + 缓存 + 术语表修补 + 漏译/退化检查 + 并发 thread_num）
- 实测（180s 切片）：Sakura-1.5B 内容覆盖 0.392 vs 3b 0.267（+47%），速度持平（15.6s），
  硬缺陷 0/0/0。样例："あなたにだけユアの秘密…" → 3b"只告诉你你一个人的秘密" /
  sak"我只告诉你我的秘密"（代词正确）
- 风险点：Sakura 许可 CC BY-NC-SA 4.0（禁商用，个人使用 OK，需标注机翻）；
  7B（4.1GB iq4xs，8G 显存推荐档）下载中，到位后 A/B

## Round 7（05:14–05:30）：Sakura 1.5B 全片正式评测

- 是否联网检索：否（复用 Round 6 结果）
- 实测（SIVR-001 全片 1254s，冷缓存）：硬缺陷 0/0/0；召回 81.5%；
  时序 −240ms（±0.5s 60.4%）；**内容覆盖 0.451/中位 0.429（3b 为 0.381/0.353）；
  高度一致行 16.4% vs 4.1%（4 倍）**；长度比 1.00；全片 95.5s（3b 131s）
- 结论：Sakura 1.5B 全面优于 qwen2.5:3b（质量、速度双优），领域微调价值实证
- 风险点：许可 NC（个人用 OK）；意译风格偶有增译

## Round 7 补充（05:35）：Sakura-7B 受阻于磁盘空间

- ollama create 7B 需要在 C:\ 循环写入 8.5GB+（blob 拷贝 + llama-quantize 验证各一份），
  C 盘仅剩 ~8-12GB 且基线已 96% 满 → 两次尝试均失败，残缺 blob 已清理
- **定案：Sakura-1.5B 作为默认翻译模型**（全片 0.451 vs 3b 0.381，且更快）；
  7B 升级选项 = 用户清理 C 盘或设置 OLLAMA_MODELS 到 E 盘后重试（GGUF 已留在
  E:\Development\_ref\models-sakura\，4.25GB）

## Round 8（05:31–05:40）：dist-app 同步 + 生产服务恢复验证

- 代码变更：无新逻辑（同步 translate_engine/config 到打包目录，重启 8756）
- 生产实例抽查（180s 切片）：28 段，日文 0 / 引号 0 / 空译文 0，请求中位 0.8s
- 状态：8756 运行中（Sakura MT 模式）

## Round 9（05:33–05:45）：重打包宿主 exe（指纹修复进打包版）

- 代码变更：无新逻辑（重打包使 host_server 的缓存指纹修复进入打包 exe）
- 完整重建：build_installer.ps1 → Setup-1.0.14.exe（39.8MB）+ dist-app 全量刷新
- 状态：8756（Sakura MT）+ 8791（新宿主）+ Nexus 界面均已重启
- 风险点：宿主 exe 与 dist-app 快照必须同批产出——分开升级会出现"宿主新指纹、
  服务旧代码"的错位

## Round 10（05:45–05:55）：生产浸泡测试 + 缓存回路验证

- 生产 8756 全片浸泡：94.6s（9.6× 实时）、0 空译文、内存 830→832MB（+2MB，无泄漏）
- 缓存存取回路：save 146 段 → load hit=True ✓；修改 dist-app 管线代码 → hit=False ✓
  （指纹跟随服务实际运行的快照，机制双向验证）；还原代码 → 恢复
- 代码变更：无（验证轮）
- 风险点：无
